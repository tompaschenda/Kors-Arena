using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Common;
using Listen;
using System.IO;

namespace WarhammerGUI
{
    /// <summary>
    /// Eine Klasse, die eine Spieler-Armee in eine kompilierbare Tex-Datei überführt!
    /// </summary>
    public class armyToTex
    {
        /// <summary>
        /// Konstruktor
        /// </summary>
        public armyToTex() {}

        /// <summary>
        /// Schreibt in einen bereits geöffneten Filestream den Latex-Code!
        /// </summary>
        /// <param name="str"></param>
        /// <returns></returns>
        public FileStream generateTexFile(FileStream str)
        {
            // Zunächst deklariere ich mir hier Strings, die Latex-Befehle symbolisieren,
            // die ich immer wieder nutzen werde!
            


            // Erstellung der benötigten Packages!
            str.

            return str;
        }
    }
}
