<UserControl x:Class="WarhammerGUI.Gui.ControlEinheitEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:Controls="clr-namespace:WarhammerGUI.Gui"
             xmlns:local="clr-namespace:WarhammerGUI.Gui"
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="AuswahlTemplate1AusN">
            <Controls:ControlAuswahl1AusN DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource TemplatedParent} }" HorizontalAlignment="Left" ></Controls:ControlAuswahl1AusN>
        </DataTemplate>
        <DataTemplate x:Key="AuswahlTemplateMAusN">
            <Controls:ControlAuswahlMAusN DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource TemplatedParent} }" HorizontalAlignment="Left" ></Controls:ControlAuswahlMAusN>
        </DataTemplate>
        <DataTemplate x:Key="AuswahlTemplateAnzahl">
            <Controls:ControlAuswahlAnzahl 
                DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource TemplatedParent} }" HorizontalAlignment="Left" 
                   
                ></Controls:ControlAuswahlAnzahl>
            
        </DataTemplate>
        <local:AuswahlTemplateSelector x:Key="auswahlTemplateSelector" 
                                       Auswahl1AusN="{StaticResource AuswahlTemplate1AusN}"
                                       AuswahlMAusN="{StaticResource AuswahlTemplateMAusN}"
                                       AuswahlSlider="{StaticResource AuswahlTemplateAnzahl}"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel  >
            <TextBox Text="{Binding Path=GUIName, Mode=OneWay}" Name="nameBox"  BorderBrush="Black" FontSize="12" FontWeight="Bold" Height="24" HorizontalContentAlignment="Center" IsReadOnly="True" TextAlignment="Center" VerticalContentAlignment="Center" />
            <ListBox Height="300" Name="listBoxChoices2" ItemsSource="{Binding Auswahlen}" 
                     ItemTemplateSelector="{StaticResource auswahlTemplateSelector}"
                     Controls:ControlAuswahlAnzahl.ValueChanged ="ControlAuswahlAnzahl_ValueChanged">
            </ListBox>
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Punkte Einheit:" Height="28" Name="label4" Width="204" />
                <TextBlock Grid.Row="0" Grid.Column="1" Height="23" Name="textEinheitKostenJetzt" Text="" Width="95" />
                <Label Grid.Row="1" Grid.Column="0" Content="Punkte Auswahl:" Height="28" Name="label1" Width="204" />
                <TextBlock Grid.Row="1" Grid.Column="1"  Height="23" Name="textAuswahlExtrakosten" Text="" Width="95" />
                <Label Grid.Row="2" Grid.Column="0" Content="Punkte Armee:" Height="28" Margin="6,6,0,0" Name="label2" Width="204" />
                <TextBlock Grid.Row="2" Grid.Column="1" Height="23" Name="textArmeeKostenJetzt" Text="" Width="95" />
                
            </Grid>
        </StackPanel>
        
    </Grid>
</UserControl>
